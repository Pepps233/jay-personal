---
import BlogPostList from '../blog/BlogPostList.astro';
import type { BlogPostGroup } from '../../lib/blog';

interface Props {
  posts: BlogPostGroup[];
  title?: string;
}

const { posts = [], title = 'All posts' } = Astro.props as Props;
---

<BlogPostList posts={posts} title={title} />
