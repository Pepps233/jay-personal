---
interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<div class={['pixel-divider', className].filter(Boolean).join(' ')} aria-hidden="true">
  <span></span>
  <span></span>
  <span></span>
</div>
